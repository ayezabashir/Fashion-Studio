import styled from "styled-components"

const Section = styled.section`
        min-height:100vh;
        position:relative;
        overflow:hidden;
`
const About = () => {
    return (
        <Section>
            about component
        </Section>
    )
}

export default About
